<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 400 120" width="400" height="120">
  <!-- Background -->
  <rect width="400" height="120" fill="#080808"/>

  <!-- Orange accent bar -->
  <rect x="0" y="0" width="6" height="120" fill="#FF4500"/>

  <!-- Za. -->
  <text
    x="24"
    y="78"
    font-family="'Bebas Neue', Impact, sans-serif"
    font-size="72"
    font-weight="400"
    letter-spacing="2"
    fill="#FF4500"
  >Za.</text>

  <!-- ally -->
  <text
    x="130"
    y="78"
    font-family="'Bebas Neue', Impact, sans-serif"
    font-size="72"
    font-weight="400"
    letter-spacing="2"
    fill="#F2F0EB"
  >ally</text>

  <!-- Errands (smaller, below) -->
  <text
    x="25"
    y="104"
    font-family="'Space Grotesk', system-ui, sans-serif"
    font-size="16"
    font-weight="600"
    letter-spacing="8"
    fill="#666666"
  >ERRANDS</text>

  <!-- Hazard dot -->
  <circle cx="382" cy="16" r="8" fill="#FF4500" opacity="0.9"/>
  <circle cx="382" cy="16" r="4" fill="#FF4500"/>
</svg>
